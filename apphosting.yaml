# Firebase App Hosting configuration
# https://firebase.google.com/docs/app-hosting

runConfig:
  # Runtime settings
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 2
  concurrency: 80

env:
  - variable: NODE_ENV
    value: production
